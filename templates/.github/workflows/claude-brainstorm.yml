# Auto-generated by claude-sdlc-kit. Safe to delete this file to uninstall;
# safe to edit `on:` triggers if needed. The `uses:` line is rewritten by
# `npx claude-sdlc-kit update` — do not edit manually unless you also remove
# this file from .claude/.kit-manifest.json.
name: Claude SDLC — Brainstorm

on:
  issues:
    types: [opened, labeled]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  brainstorm:
    uses: kolodii-ivan/claude-sdlc-kit/.github/workflows/brainstorm.yml@__PIN__
    secrets: inherit
