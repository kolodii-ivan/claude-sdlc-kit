# Auto-generated by claude-sdlc-kit. See claude-brainstorm.yml header for edit rules.
name: Claude SDLC — Implement

on:
  repository_dispatch:
    types: [spec-ready]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write
  actions: write

jobs:
  implement:
    uses: kolodii-ivan/claude-sdlc-kit/.github/workflows/implement.yml@__PIN__
    secrets: inherit
