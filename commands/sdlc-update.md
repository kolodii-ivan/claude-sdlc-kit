---
description: Refresh kit-managed files (SHA-tracked; preserves local edits)
---

Run:

```bash
npx --yes @kolodii-ivan/claude-sdlc-kit update $ARGUMENTS
```

Then summarize the output for the user, especially any "skipped (modified locally)" lines and offer to investigate them.
